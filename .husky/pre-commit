#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

echo "🟢 Linting all TS/JS files..."
npx eslint "src/**/*.{ts,js}" --fix

echo "🎨 Formatting all files with Prettier..."
bunx prettier . --write

git add .
